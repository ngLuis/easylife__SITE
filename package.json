{
  "name": "proyecto_cliente",
  "version": "1.0.0",
  "description": "proyecto parte  cliente",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build && sh ./scripts/deploy.sh",
    "build": "sh ./scripts/build.sh",
    "serve": "onchange ./src/**/* -- npm run deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^7.0.0",
    "onchange": "^6.1.0"
  }
}
